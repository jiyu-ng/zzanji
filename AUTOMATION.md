# 짠지 💰 — 자동 기능개선 가이드

짠지는 대표님 **본인용** 가계부 웹앱(Vite + React + Supabase, GitHub Pages `jiyu-ng.github.io/zzanji`).
키리의 로테이션 자동개선 크론이 이 파일을 기준으로 **작은 개선 1건**을 골라 구현한다.

## 성격·우선순위 (⚠️ 본인용 앱)

- 광고수익·SEO가 목적이 아님 → **입력 편의·안정성·품질**이 우선.
- 우선순위: **입력/조회 UX · 접근성 · 성능 · 버그 수정 · 코드품질 · 모바일 대응**.
- ⚠️ **핵심 기능 로직은 자동으로 건드리지 않는다** — 가계부 데이터 CRUD, Supabase 스키마·쿼리(`ledger` 등), 집계·통계 계산, 인증 흐름. 잘못 손대면 데이터·집계가 틀어지고 티가 크게 남.

## ✅ 자동 머지 OK (빌드 통과 + 기존 기능 안 깨짐)

작고 안전한 개선만:
- **UI/스타일**: 레이아웃·간격·반응형(모바일 입력), 폼·버튼·리스트 가독성
- **접근성**: aria, 포커스, 키보드(숫자 입력 편의), 색 대비, 시맨틱 태그
- **UX 소품**: 로딩·빈 상태·저장 완료 피드백, 에러 문구, 입력 검증 '표시'(로직 변경 없이)
- **성능**: 불필요 리렌더 정리, 번들 소소한 최적화
- **코드품질**: 죽은 코드 제거, 네이밍, 주석, 린트/포맷, 의존성 **패치**

**공통 조건**: `npm run build` 통과 + **기존 기능이 그대로 동작**(입력·저장·조회·집계 안 깨짐) + 변경 범위 작음. 하나라도 애매하면 리뷰行.

## 🙋 리뷰行 (머지 X, PR 열어두고 DM 보고)

- 기능 로직·상태관리·집계 계산 변경, 새 기능/화면 추가
- Supabase 스키마·쿼리·RLS, 데이터 모델 변경
- 인증/환경변수, 의존성 major 업데이트, 빌드·배포 설정
- 디자인 인상이 바뀌는 변화
- **조금이라도 데이터·집계·동작에 영향 가면 전부 리뷰行** (기본값)

## 작업 흐름

1. `git checkout main && git pull`
2. 개선 1건 구현 + `npm run build` 통과 확인 (가능하면 입력·조회 화면 동작도 눈으로 점검)
3. `auto/feat-<slug>` 브랜치 커밋·push, `gh pr create`
4. '빌드 통과 + 기존 기능 안 깨짐 + 작은 범위'면 자동머지, 아니면 리뷰行 → DM 보고
5. 마땅한 개선거리 없으면 조용히 패스(보고 생략)

## ROADMAP (개선 백로그 — 처리하면 [x], 새 아이디어 추가)

- [ ] 입력 폼 UX: 모바일 숫자 키패드, 포커스 이동, 저장 피드백('표시'만)
- [ ] 목록·대시보드 가독성(정렬·간격·빈 상태 문구)
- [ ] 접근성: aria, 키보드, 색 대비
- [ ] 불필요한 리렌더·번들 최적화(기능 불변)
- [ ] 코드 정리: 순수 UI 컴포넌트 분리, 죽은 코드·경고 제거
- [ ] 파비콘·메타·타이틀 등 기본 마감
